<UserControl x:Class="Wpf_Base.TestWpf.FileIoDemo"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Wpf_Base.TestWpf"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Vertical">
            <Button Content="写入 ini 文件" Margin="5" Click="ButtonWriteIni_Click"/>
            <Button Content="读取 ini 文件" Margin="5" Click="ButtonReadIni_Click"/>
            <Button Content="写入 xml 文件" Margin="5" Click="ButtonWriteXml_Click"/>
            <Button Content="读取 xml 文件" Margin="5" Click="ButtonReadXml_Click"/>
            <Button Content="读取 xls 文件" Margin="5" Click="ButtonReadXls_Click"/>
            <Button Content="读取 xlsx 文件" Margin="5" Click="ButtonReadXlsx_Click"/>
        </StackPanel>
        <ListBox Grid.Row="1" x:Name="LB_Info" Height="200"/>
    </Grid>
</UserControl>